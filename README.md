# Pulser presentations
Edited notebooks for live Pulser demo, based on notebooks in Pulser repo.

When running locally, install environment from `requirements.txt` using `pip install -r requirements.txt`.
